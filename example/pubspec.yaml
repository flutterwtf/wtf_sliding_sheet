name: example
description: An example implementation for a SlidingSheet.
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=2.19.0 <4.0.0"

dependencies:
  community_charts_flutter: ^1.0.1

  flutter:
    sdk: flutter

  material_design_icons_flutter: ^7.0.7296
  wtf_sliding_sheet:
    path: ../

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/maps_screenshot.png